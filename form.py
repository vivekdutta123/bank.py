filename = input("Enter the filename: ")

fh = open(filename,'r')
for line in fh:
    fx = line.rstrip()
    if line startwith("from")
    word=line.split()
    for word in words:
       for word in words[1:]:
                print(word)
